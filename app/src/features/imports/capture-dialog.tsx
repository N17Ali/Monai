import { ClipboardIcon, ReceiptIcon } from "@hugeicons/core-free-icons";
import { HugeiconsIcon } from "@hugeicons/react";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function CaptureDialog({ open, onOpenChange, onChoose }: { open: boolean; onOpenChange: (value: boolean) => void; onChoose: (choice: "clipboard" | "manual") => void }) {
  return <Dialog onOpenChange={onOpenChange} open={open}><DialogContent className="lg:w-[440px]"><DialogHeader><DialogTitle>ثبت تراکنش</DialogTitle><DialogDescription>روش ورود اطلاعات را انتخاب کنید.</DialogDescription></DialogHeader><div className="mt-5 grid gap-3"><button aria-label="خواندن پیام بانکی" className="choice-card choice-card-primary flex min-h-16 items-center gap-3 rounded-lg border border-primary bg-primary-soft p-3 text-start transition-[background-color,border-color,transform] duration-150 active:scale-[0.97]" onClick={() => onChoose("clipboard")}><HugeiconsIcon className="text-primary" icon={ClipboardIcon} size={24} /><span><strong className="block text-sm">خواندن پیام بانکی</strong><small className="mt-1 block text-muted-foreground">پیام کپی‌شده را سریع وارد کن</small></span></button><button aria-label="ثبت" className="choice-card flex min-h-16 items-center gap-3 rounded-lg border border-border p-3 text-start transition-[background-color,border-color,transform] duration-150 active:scale-[0.97]" onClick={() => onChoose("manual")}><HugeiconsIcon className="text-primary" icon={ReceiptIcon} size={24} /><span><strong className="block text-sm">ثبت</strong><small className="mt-1 block text-muted-foreground">اگر پیام بانکی در دسترس نیست</small></span></button></div><p className="mt-4 rounded-lg bg-surface-muted p-3 text-xs leading-6 text-muted-foreground">پیام فقط برای استخراج اطلاعات تراکنش بررسی می‌شود.<br />پیام‌های حاوی رمز ذخیره نمی‌شوند.</p></DialogContent></Dialog>;
}
